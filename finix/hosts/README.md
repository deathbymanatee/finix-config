# hosts

Each folder is a configuration for individual host machines. Mapped directly to each computer's hostname. It has both a configuration.nix and a hardware-configuration.nix, the hardware config is (mostly) what differs per machine.
